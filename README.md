# Django_Scraping_Api
First terminal : redis-server
Second terminal :celery -A scraping_project worker --loglevel=info
Third terminal : python run.py
For POST a request in selector section , it should be like :
[
    "h1",
    "h2"
]
